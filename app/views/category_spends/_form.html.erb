<%= form_with(model: category_spend) do |form| %>
  <% if category_spend.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_spend.errors.count, "error") %> prohibited this category_spend from being saved:</h2>

      <ul>
        <% category_spend.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
